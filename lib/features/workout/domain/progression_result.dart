class ProgressionResult {
  final double weight;
  final int reps;
  final String reason;

  const ProgressionResult({
    required this.weight,
    required this.reps,
    required this.reason,
  });
}